import { defineConfig } from 'vite'

export default defineConfig({
  // GitHub Pages のプロジェクトページにデプロイする場合は
  // リポジトリ名に合わせてください (例: '/web-api-js/')
  // ユーザーページ (username.github.io) の場合は '/' のままで OK
  base: './',
})
